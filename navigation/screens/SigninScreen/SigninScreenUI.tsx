import { View, Text } from 'react-native';

interface SigninScreenProps {}

const SigninScreenUI = (props: SigninScreenProps) => {
  const {} = props;

  return (
    <View>
      <Text>SigninScreenUI</Text>
    </View>
  );
};

export default SigninScreenUI;
