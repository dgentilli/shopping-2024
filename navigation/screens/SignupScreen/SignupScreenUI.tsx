import { View, Text } from 'react-native';

interface SignupScreenProps {}

const SignupScreenUI = (props: SignupScreenProps) => {
  const {} = props;

  return (
    <View>
      <Text>SignupScreenUI</Text>
    </View>
  );
};

export default SignupScreenUI;
